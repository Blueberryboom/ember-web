import type { MetadataRoute } from 'next'; import { siteConfig } from '@/lib/config'; const routes=['','/pricing','/docs','/docs/getting-started','/docs/commands','/docs/premium','/docs/configuration','/docs/api','/docs/faq','/privacy','/terms']; export default function sitemap():MetadataRoute.Sitemap{return routes.map(route=>({url:`${siteConfig.url}${route}`,lastModified:new Date()}))}
