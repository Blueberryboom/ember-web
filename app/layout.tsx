import type { Metadata, Viewport } from 'next'; import './globals.css'; import { Navbar } from '@/components/navbar'; import { RootProvider } from 'fumadocs-ui/provider/next'; import { Footer } from '@/components/footer'; import { siteConfig } from '@/lib/config';
export const metadata: Metadata = { metadataBase:new URL(siteConfig.url), title:{default:'Ember — Discord moderation & utility',template:'%s | Ember'}, description:siteConfig.description, alternates:{canonical:'/'}, openGraph:{type:'website',siteName:'Ember',title:'Ember — Discord moderation & utility',description:siteConfig.description,url:siteConfig.url}, twitter:{card:'summary',title:'Ember — Discord moderation & utility',description:siteConfig.description}, icons:{icon:'/icon.svg'} };
export const viewport: Viewport = { themeColor:'#100d0d' };
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><RootProvider><Navbar/><main>{children}</main><Footer/></RootProvider></body></html>}
